const notas = [10, 6, 8];


// Função para adicionar valor na array
notas.push(7);

// Somente para ver se foi adicionado a nota
//console.log(notas);

const media = (notas[0] + notas [1] + notas[2] + notas[3]) / notas.length;

console.log(media);
